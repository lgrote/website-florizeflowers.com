---
export interface SeasonInfo {
  season: string;
  icon: string;
  months: string;
  popularFlowers: string[];
  availability: "Excellent" | "Good" | "Fair" | "Limited";
  pricing: "Budget-friendly" | "Moderate" | "Premium" | "Variable";
  tips: string[];
}

export interface Props {
  seasons: SeasonInfo[];
  title?: string;
}

const {
  seasons,
  title = "Seasonal Flower Guide"
} = Astro.props;

const availabilityColors = {
  "Excellent": "#10b981",
  "Good": "#3b82f6",
  "Fair": "#f59e0b",
  "Limited": "#ef4444"
};

const pricingColors = {
  "Budget-friendly": "#10b981",
  "Moderate": "#3b82f6",
  "Premium": "#f59e0b",
  "Variable": "#6b7280"
};
---

<div class="seasonal-calendar">
  <h3 class="seasonal-title">{title}</h3>
  <div class="seasons-grid">
    {seasons.map((season) => (
      <div class="season-card">
        <div class="season-header">
          <div class="season-icon">{season.icon}</div>
          <h4 class="season-name">{season.season}</h4>
          <p class="season-months">{season.months}</p>
        </div>
        <div class="season-content">
          <div class="season-flowers">
            <span class="content-label">Popular Flowers:</span>
            <ul class="flowers-list">
              {season.popularFlowers.map((flower) => (
                <li>{flower}</li>
              ))}
            </ul>
          </div>
          <div class="season-indicators">
            <div class="indicator">
              <span class="indicator-label">Availability:</span>
              <span
                class="indicator-badge"
                style={`background-color: ${availabilityColors[season.availability]}`}
              >
                {season.availability}
              </span>
            </div>
            <div class="indicator">
              <span class="indicator-label">Pricing:</span>
              <span
                class="indicator-badge"
                style={`background-color: ${pricingColors[season.pricing]}`}
              >
                {season.pricing}
              </span>
            </div>
          </div>
          {season.tips.length > 0 && (
            <div class="season-tips">
              <span class="content-label">Tips:</span>
              <ul class="tips-list">
                {season.tips.map((tip) => (
                  <li>{tip}</li>
                ))}
              </ul>
            </div>
          )}
        </div>
      </div>
    ))}
  </div>
</div>

<style>
  .seasonal-calendar {
    background: linear-gradient(135deg, #fffbeb 0%, #ffffff 100%);
    border: 1px solid #fde68a;
    border-radius: 16px;
    padding: 2rem;
    margin: 2rem 0;
  }

  .seasonal-title {
    font-size: 1.5rem;
    font-weight: 700;
    color: var(--florize-green, #4a7c59);
    text-align: center;
    margin: 0 0 2rem 0;
  }

  .seasons-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 1.5rem;
  }

  .season-card {
    background: white;
    border-radius: 12px;
    overflow: hidden;
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.06);
    transition: all 0.3s;
  }

  .season-card:hover {
    transform: translateY(-4px);
    box-shadow: 0 8px 16px rgba(74, 124, 89, 0.15);
  }

  .season-header {
    background: linear-gradient(135deg, #f0f8f4 0%, #e8f5e9 100%);
    padding: 1.5rem;
    text-align: center;
  }

  .season-icon {
    font-size: 3rem;
    margin-bottom: 0.5rem;
  }

  .season-name {
    font-size: 1.25rem;
    font-weight: 700;
    color: var(--florize-green, #4a7c59);
    margin: 0 0 0.25rem 0;
  }

  .season-months {
    font-size: 0.875rem;
    color: #6b7280;
    margin: 0;
  }

  .season-content {
    padding: 1.5rem;
    display: flex;
    flex-direction: column;
    gap: 1.25rem;
  }

  .content-label {
    font-size: 0.75rem;
    font-weight: 600;
    color: #9ca3af;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    display: block;
    margin-bottom: 0.5rem;
  }

  .season-flowers {
  }

  .flowers-list {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }

  .flowers-list li {
    font-size: 0.875rem;
    color: #374151;
    padding-left: 1.25rem;
    position: relative;
  }

  .flowers-list li::before {
    content: "🌸";
    position: absolute;
    left: 0;
  }

  .season-indicators {
    display: flex;
    gap: 1rem;
  }

  .indicator {
    flex: 1;
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }

  .indicator-label {
    font-size: 0.75rem;
    font-weight: 600;
    color: #9ca3af;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .indicator-badge {
    display: inline-block;
    padding: 0.375rem 0.75rem;
    border-radius: 6px;
    color: white;
    font-size: 0.75rem;
    font-weight: 600;
    text-align: center;
  }

  .season-tips {
  }

  .tips-list {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }

  .tips-list li {
    font-size: 0.875rem;
    color: #374151;
    padding-left: 1.25rem;
    position: relative;
    line-height: 1.5;
  }

  .tips-list li::before {
    content: "💡";
    position: absolute;
    left: 0;
  }

  @media (max-width: 1024px) {
    .seasons-grid {
      grid-template-columns: repeat(2, 1fr);
      gap: 1.25rem;
    }
  }

  @media (max-width: 768px) {
    .seasonal-calendar {
      padding: 1.5rem;
    }

    .seasonal-title {
      font-size: 1.25rem;
      margin-bottom: 1.5rem;
    }

    .seasons-grid {
      grid-template-columns: 1fr;
      gap: 1.25rem;
    }

    .season-header {
      padding: 1.25rem;
    }

    .season-icon {
      font-size: 2.5rem;
    }

    .season-content {
      padding: 1.25rem;
    }
  }
</style>
