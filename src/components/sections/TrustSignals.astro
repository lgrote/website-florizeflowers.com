---
import Card from '../ui/Card.astro';
import type { SupportedLanguage } from '../../i18n/config';
import { getHomepageSection } from '../../lib/sanity-fetchers';

export interface Props {
  title?: string;
  signals?: Array<{
    title: string;
    description: string;
    icon?: string;
  }>;
  className?: string;
  lang?: SupportedLanguage;
}

const {
  title,
  signals,
  className = '',
  lang = 'en' as SupportedLanguage
} = Astro.props;

// Read from Sanity if props are not provided
const trustSignalsData = await getHomepageSection('trust-signals', lang);

const sectionTitle = title || trustSignalsData?.title || 'Why Trust Us';
const defaultSignals = signals || trustSignalsData?.cards || [];
---

<section class={`section-padding bg-cream-white ${className}`}>
  <div class="container">
    <div class="max-w-3xl mx-auto text-center">
      <h2 class="text-3xl font-bold text-florize-green mb-6">
        {sectionTitle}
      </h2>

      <div class="grid grid-cols-1 md:grid-cols-2 gap-8 mt-8">
        {defaultSignals.map((signal) => (
          <Card>
            <h3 class="text-xl font-bold mb-3 text-florize-green">
              {signal.title}
            </h3>
            <p class="text-gray-700">
              {signal.description}
            </p>
          </Card>
        ))}
        <slot />
      </div>
    </div>
  </div>
</section>
