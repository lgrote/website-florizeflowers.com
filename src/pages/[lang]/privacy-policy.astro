---
import BaseLayout from '../../layouts/BaseLayout.astro';
import PortableTextRenderer from '../../components/PortableTextRenderer.astro';
import { SUPPORTED_LANGUAGES, type SupportedLanguage } from '../../i18n/config';
import { getPageBySlug } from '../../lib/sanity-fetchers';

export async function getStaticPaths() {
  return SUPPORTED_LANGUAGES.map((lang) => ({
    params: { lang },
  }));
}

const { lang } = Astro.params as { lang: SupportedLanguage };

const privacyPage = await getPageBySlug('privacy-policy', lang);

if (!privacyPage) {
  throw new Error(`Privacy policy page content not found for language: ${lang}`);
}
---

<BaseLayout
  title={privacyPage.title}
  description={privacyPage.description}
>
  <div class="py-12">
    <div class="container">
      <div class="max-w-3xl mx-auto prose prose-lg">
        <PortableTextRenderer value={privacyPage.body} />
      </div>
    </div>
  </div>
</BaseLayout>
